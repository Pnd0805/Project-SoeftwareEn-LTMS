/** แต้มในตาราง tournament_standings ต่อ 1 ชนะ — ใช้ทั้งผลปกติ (verifyMatchResult) และ walkover ให้ตรงกัน */
export const WIN_POINTS = 3;

/**
 * OD-26 ข้อ 7 (มติ 26 ก.ย.) — ผลที่ส่งแล้วไม่มีใครค้าน ระบบยืนยันให้เองหลังกี่ชั่วโมง
 * เป็นกลไกความปลอดภัยของระบบ จึงเป็นค่าคงที่ ผู้จัดปรับไม่ได้ (ต่างจาก dispute_window_hours ที่ผู้จัดตั้งได้)
 * ใช้เป็นเพดานเมื่อไม่มีแมตช์ถัดไปมากดดัน เช่น รอบชิงหรือ round robin
 */
export const AUTO_VERIFY_HOURS = 24;

/** ต้องยืนยันผลให้เสร็จก่อนแมตช์ถัดไปเริ่มอย่างน้อยเท่านี้ — เผื่อเวลาให้สายเดินทัน */
export const AUTO_VERIFY_LEAD_MINUTES = 15;

/**
 * OD-26 ข้อ 6 — แมตช์จบแล้วไม่มีใครส่งผลนานเท่านี้ ถึงจะเปิดบันไดสำรอง
 * ขั้นแรก: กรรมการของแมตช์ส่งแทนได้ (รวมโหมด online ที่ปกติส่งไม่ได้) · ขั้นสอง: ผู้จัดตัดสินเอง
 * ใช้ตัวเลขเดียวกับ AUTO_VERIFY_HOURS โดยตั้งใจ — จะได้มีนาฬิกาชนิดเดียวให้จำ
 */
export const SUBMIT_ESCALATION_HOURS = 24;

/**
 * OD-23 (แก้ 26 ก.ย.) — โหวต MVP รายแมตช์เปิดทันทีที่แมตช์จบ (`matches.actual_end_time`) และปิดหลังจากนั้นกี่ชั่วโมง
 * ผูกกับเวลาจบจริงของแมตช์ ไม่ใช่เวลาปิดทัวร์ — คนดูจะได้โหวตตอนที่ยังจำเกมได้
 */
export const MVP_VOTING_HOURS = 24;

/**
 * OD-26 ข้อ 10 — ผู้จัดมีเวลาเท่านี้ในการตัดสินข้อโต้แย้ง ครบแล้วแอดมินมหาวิทยาลัยกดได้ด้วย
 * เป็นการ "เพิ่มคนที่กดได้" ไม่ใช่โอนอำนาจ — ผู้จัดยังกดได้ตลอด ใครถึงก่อนได้ก่อน
 * จำเป็นเพราะรอบชิงกับ round robin ไม่มีแมตช์ถัดไปให้บล็อก แรงกดดันจึงไปไม่ถึงผู้จัดที่หายไป
 */
export const ORG_RESOLVE_HOURS = 48;

